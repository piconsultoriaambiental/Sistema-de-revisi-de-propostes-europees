{
  "name": "pi-consultoria-revisions",
  "version": "1.0.0",
  "description": "Sistema de revisió de propostes europees per a PI Consultoria",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "stripe": "^14.0.0",
    "resend": "^2.0.0"
  },
  "engines": {
    "node": "24.x"
  }
}
